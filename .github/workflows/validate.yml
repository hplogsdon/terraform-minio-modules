name: Validate

on:
  pull_request:
    branches:
      - trunk
      - master

env:
  TERRAFORM_DOCS_VERSION: v0.19.0
  TFLINT_VERSION: v0.53.0

jobs:
  collect_inputs:
    name: Collect Workflow Input
    runs-on: ubuntu-latest
    outputs:
      directories: ${{ steps.dirs.outputs.directories }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Get Root
        id: dirs
        uses: clowdhaus/terraform-composite-actions/directories@v1.9.0

  get_versions:
    name: Extract Terraform Min/Max Versions
    needs: collect_inputs
    runs-on: ubuntu-latest
    strategy:
      matrix:
        directory: ${{ fromJson(needs.collect_inputs.outputs.directories) }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get Terraform Min/Max Versions
        id: minMax
        uses: clowdhaus/terraform-min-max@v1.3.1
        with:
          directory: ${{ matrix.directory }}

  validate_min_versions:
    name: Minimum TF Versions
    needs: get_versions
    runs-on: ubuntu-latest
    strategy:
      matrix:
        directory: ${{ fromJson(needs.collect_inputs.outputs.directories) }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Terraform ${{ needs.get_versions.outputs.minVersion }} Modules
        if: ${{ matrix.directory != '.' }}
        uses: clowdhaus/terraform-composite-actions/pre-commit@v1.11.1
        with:
          terraform-version: ${{ needs.get_versions.outputs.minVersion }}
          tflint-version: ${{ env.TFLINT_VERSION }}
          args: 'terraform_validate --color=always --show-diff-on-failure --files ${{ matrix.directory }}/*'

      - name: Validate Terraform ${{ needs.get_versions.outputs.minVersion }} Root
        if: ${{ matrix.directory == '.' }}
        uses: clowdhaus/terraform-composite-actions/pre-commit@v1.11.1
        with:
          terraform-version: ${{ needs.get_versions.outputs.minVersion }}
          tflint-version: ${{ env.TFLINT_VERSION }}
          args: 'terraform_validate --color=always --show-diff-on-failure --files $(ls *.tf)'

  validate_max_versions:
    name: Maximum TF Versions
    needs: get_versions
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: '${{ github.event.pull_request.head.repo.full_name }}'

      - name: Validate Terraform ${{ needs.get_versions.outputs.maxVersion }}
        uses: clowdhaus/terraform-composite-actions/pre-commit@v1.11.1
        with:
          terraform-version: ${{ needs.get_versions.outputs.maxVersion }}
          tflint-version: ${{ env.TFLINT_VERSION }}
          terraform-docs-version: ${{ env.TERRAFORM_DOCS_VERSION }}
          install-hcledit: true
